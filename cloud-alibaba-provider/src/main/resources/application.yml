spring:
  application:
    name: service-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.6.101:8848
server:
  port: 8001

management:
  endpoints:
    web:
      exposure:
        exclude: "*"